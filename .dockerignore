# Ignore files that shouldn't be in Docker image
.git
.gitignore
.bundle
log/*
tmp/*
storage/*
node_modules
vendor/bundle
.sass-cache
*.log
.env
.env.local
.env.*.local
coverage
spec
test
*.md
!README.md
Dockerfile.dev
docker-compose*.yml
RAILWAY_DEPLOY.md

# IMPORTANT: Do NOT ignore core and admin engines - they are needed for bundle install
# These directories must be included in the Docker build
