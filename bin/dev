#!/usr/bin/env sh

if gem list --no-installed --exact --silent foreman; then
  echo "Installing foreman..."
  gem install foreman
fi

# Default to port 3000 if not specified
export PORT="${PORT:-3000}"

ENV_FILE="/dev/null"
if [ -f ".env" ]; then
  ENV_FILE=".env"
fi

exec foreman start -f Procfile.dev --env "$ENV_FILE" "$@"
