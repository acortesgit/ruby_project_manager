[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# Railway will use Procfile automatically for multiple processes
# The Procfile defines: web (with migrations) and worker (Sidekiq)
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

